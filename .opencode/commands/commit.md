---
description: Commit changes with an AI-generated message
---
Here is the current git status and diff:

!`git status`

!`git diff --staged`

Analyze the changes above and generate a concise, descriptive commit message in Russian. Then commit using:
```
git commit -m "<message>"
```
